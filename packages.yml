# dbt package dependencies. Installed by dbt deps (run in CI before build).
# dbt_utils is the standard helper package from dbt Labs which provides
# the generic `expression_is_true` test used on the staging model .
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
