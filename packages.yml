# dbt package dependencies. Installed by `dbt deps` (run in CI before build).
# dbt_utils is the de-facto standard helper package from dbt Labs — it provides
# the generic `expression_is_true` test used on the staging model below.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
